import { Meta } from '@storybook/blocks';

<Meta title="Introduction" />

# Deepfluid Design System

A React implementation of the Deepfluid DS Figma library.

## Browse

- **Components** — every component, with all variants, states, and the Figma link
- **Tokens** _(this section)_ — colors, spacing, typography reference

Use the toolbar at the top to switch between **light** and **dark** themes.

---

## Color tokens

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))', gap: 12, margin: '24px 0' }}>
  {[
    { name: '--fill-primary-default', label: 'Primary' },
    { name: '--fill-primary-hover', label: 'Primary hover' },
    { name: '--fill-secondary-default', label: 'Secondary' },
    { name: '--fill-danger-default', label: 'Danger' },
    { name: '--fill-warning-default', label: 'Warning' },
    { name: '--fill-success', label: 'Success' },
    { name: '--text-primary', label: 'Text primary' },
    { name: '--text-secondary', label: 'Text secondary' },
    { name: '--text-tertiary', label: 'Text tertiary' },
    { name: '--border-default', label: 'Border' },
    { name: '--border-strong', label: 'Border strong' },
    { name: '--surface-page', label: 'Surface' },
  ].map((t) => (
    <div key={t.name} style={{ border: '1px solid var(--border-subtle)', borderRadius: 8, overflow: 'hidden' }}>
      <div style={{ height: 48, background: `var(${t.name})` }} />
      <div style={{ padding: 8, fontSize: 12, fontFamily: 'var(--font-family-sans)' }}>
        <div style={{ fontWeight: 600 }}>{t.label}</div>
        <div style={{ color: 'var(--text-tertiary)' }}>{t.name}</div>
      </div>
    </div>
  ))}
</div>

## Spacing scale

<div style={{ display: 'flex', flexDirection: 'column', gap: 4, margin: '24px 0', maxWidth: 480 }}>
  {[2, 4, 6, 8, 12, 16, 20, 24, 32, 44].map((s) => (
    <div key={s} style={{ display: 'flex', alignItems: 'center', gap: 12 }}>
      <div style={{ width: 80, fontSize: 12, color: 'var(--text-tertiary)', fontFamily: 'var(--font-family-mono)' }}>
        --size-{s}
      </div>
      <div style={{ width: s, height: 16, background: 'var(--fill-primary-default)' }} />
      <div style={{ fontSize: 12, color: 'var(--text-secondary)' }}>{s}px</div>
    </div>
  ))}
</div>

## Typography

<div style={{ margin: '24px 0' }}>
  <div className="df-display-xl">Display XL · 56px</div>
  <div className="df-display-lg">Display LG · 44px</div>
  <div className="df-h1">Heading H1 · 32px</div>
  <div className="df-h2">Heading H2 · 24px</div>
  <div className="df-h3">Heading H3 · 20px</div>
  <div className="df-h4">Heading H4 · 18px</div>
  <div className="df-subheader-lg">Subheader Large · 16px</div>
  <div className="df-subheader-md">Subheader Medium · 14px</div>
  <div className="df-body-lg">Body Large · 16px — The quick brown fox jumps over the lazy dog.</div>
  <div className="df-body-md">Body Medium · 14px — The quick brown fox jumps over the lazy dog.</div>
  <div className="df-caption">Caption · 12px — Lorem ipsum dolor sit amet.</div>
</div>
